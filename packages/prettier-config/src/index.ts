import { Config } from "prettier";

const config: Config = {
  semi: true,
  trailingComma: "all",
  printWidth: 80,
  endOfLine: "auto",
  singleQuote: false,
  tabWidth: 2,
  useTabs: false,
  bracketSpacing: true,
};

module.exports = config;
